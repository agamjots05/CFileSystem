#ifndef FILEOPS
#define FIlEOPS

#include "types.h"

#include <stdio.h>

int createFile(const char *name);
int findFile(const char *name, FILE *fp);

int openFile(const char *name);

    
#endif // !FILEOPS
#define FIlEOPS

